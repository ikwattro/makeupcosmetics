{% extends "StoreFrontBundle:Default:layout.html.twig" %}

{% trans_default_domain 'Interface' %}

{% block content %}

    <div class="row">
        <div class="col-md-6">
            <h3>{{ 'account.already_one'|trans|capitalize }}</h3>

            {{ render(controller('StoreFrontBundle:Checkout:login')) }}
        </div>
        <div class="col-md-6">
            <h3>{{ 'account.not_yet'|trans|capitalize }}</h3>
            <a href="{{ path('fos_user_registration_register') }}" class="btn btn-success">{{ 'account.create'|trans|capitalize }}</a>
        </div>
    </div>


<hr>


{% endblock %}