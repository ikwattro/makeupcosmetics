{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Promotion creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('promotion') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
